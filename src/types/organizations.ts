/** Organization structure with org_id and name */
export type Organization = {
  org_id: string;
  name: string;
};

/** Project structure with project_id and name */
export type Project = {
  project_id: string;
  name: string;
};
